export HSA_OVERRIDE_GFX_VERSION=10.3.0
export ROCM_PATH="/opt/rocm"
export BUBBLETEA_LOG=/tmp/bubbletea.log

# Isso aqui acha todos os arquivos lá dentro e junta tudo num linguição separado por ":"
export KUBECONFIG=$(find ~/.kube/configs -type f | tr '\n' ':' | sed 's/:$//')
